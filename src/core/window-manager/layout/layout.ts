// http://golden-layout.com
import IContainer from './../icontainer'
/**
 * tile 包含行列管理，显示的位置是由tile限制的
 * statck 窗口叠加，显示位置不受限制。可以通过group的方式，将stack分组，然后通过tab（或其他）的方式显示
 */
// impl IContainer
class Layout implements IContainer{
    
}